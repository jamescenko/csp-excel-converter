Flask==3.0.3
openpyxl==3.1.5
gunicorn==22.0.0
Werkzeug==3.0.3
```

### `Procfile`
```
web: gunicorn --bind 0.0.0.0:$PORT --workers 2 --threads 2 --timeout 120 --access-logfile - --error-logfile - app:app
